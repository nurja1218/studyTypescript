// function sum (a: number, b: number): number {
//     return a+b;
// }

// const result = sum(1,2);
// console.log(result.toLocaleString())

/**
 * 
 * @param {number} a 
 * @param {number} b 
 */
function sum (a, b) {
    return a+b;
}